---
version: 2

builds:
- env:
  - CGO_ENABLED=0
  goos:
  - linux
  - darwin
  goarch:
  - amd64
  - arm64
  main: ./cmd/yft/main.go
  flags:
  - -trimpath
  ldflags:
  - -s -w -extldflags "-static" -X github.com/homeport/yft/internal/cmd.version={{.Version}}
  mod_timestamp: '{{ .CommitTimestamp }}'

checksum:
  name_template: 'checksums.txt'

snapshot:
  version_template: "{{ .Tag }}-next"

release:
  name_template: "{{.ProjectName}} release v{{.Version}}"

changelog:
  disable: false
  use: github
  filters:
    exclude:
    - "Merge pull request"
    - "dependabot"

homebrew_casks:
- repository:
    owner: homeport
    name: homebrew-tap
    token: "{{ .Env.HOMEBREW_TAP_GITHUB_TOKEN }}"
  url:
    template: "https://github.com/homeport/yft/releases/download/{{ .Tag }}/{{ .ArtifactName }}"
  commit_author:
    name: GoReleaser Bot
    email: goreleaser@carlosbecker.com
  directory: Casks
  homepage: "https://github.com/homeport/yft"
  description: "yft - YAML file tool"
